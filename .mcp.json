{
  "mcpServers": {
    "localci": {
      "type": "stdio",
      "command": "nix",
      "args": ["run", "github:srid/localci/mcp-integration", "--", "--mcp", "--sha", "HEAD", "-f", "localci.json"]
    }
  }
}
